# 1. 确认你的 EFI 分区编号
ls

# 我的输出信息是: (proc) (memdisk) (hd0) (hd1) (hd1,gpt3) (hd1,gpt2) (hd1,gpt1) (hd2) (hd3) (lvm/ubuntu--vg-ubuntu--lv)

# 通常会显示 (hd0,gpt1) 这种，尝试找到包含 EFI 文件夹的那个
ls (hd1,gpt1)/EFI/

# 2. 链式加载工具
chainloader (hd1,gpt1)/EFI/tools/setup_var.efi

# 3. 运行
boot
