# 读取变量时，先指定区域如 Setup 或 CpuSetup
# 然后在 : 符号后面跟上Offset偏移量
# 例如，这里读取偏移量0x5c2
setup_var.efi Setup:0x5c2

# 这里我看到返回结果是 Setup:0x5c2=0x00

#============

# 如果要读取偏移量位置的2个字节，则再加上(2)
setup_var.efi Setup:0x5c2(2)

# 这里我看到返回结果是 Setup:0x5c2=0x0000

#=============

# 现在把偏移量 0x5c2 位置内容改成 0x01 表示256MB
setup_var.efi Setup:0x5c2=0x01

# 这是看到返回结果 Setup:0x5c2=0x01
# 可以再次用 setup_var.efi Setup:0x5c2 命令查看确认该位置已经写入了新数据0x01

