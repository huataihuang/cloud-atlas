# 1. 确保 PulseAudio 客户端配置指向 4713
mkdir -p ~/.config/pulse
cat << 'EOF' > ~/.config/pulse/default.pa
.include /etc/pulse/default.pa
# 建立一个虚拟的 TCP 音频出口，将声音默认塞给 127.0.0.1:4713
load-module module-tunnel-sink server=127.0.0.1:4713 sink_name=remote_mac
set-default-sink remote_mac
EOF
