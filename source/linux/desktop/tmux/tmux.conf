# 本地tmux修改色彩，以便和远程tmux默认色彩区分
set -g default-terminal "screen-256color"
set -g status-bg blue
set -g status-fg black

# 本地tmux的{prefix}前导键修改
set -g prefix C-a
unbind C-b
bind C-a send-prefix
set -g escape-time 20
