input type:touchpad {
    # 打字是自动禁用触控板(Disable-While-Typing)
    # 当检测到键盘有输入时，触控板会立即停止相应，停止敲键盘后约 500ms 自动恢复
    dwt enabled
    # 轻触即点击(Tap-to-click)
    tap enabled
    # 自然滚动(双指向上划,网页向下滑，符合macOS习惯)
    natural_scroll enabled
    # 双指轻触为右键，三指轻触为中键
    tap_button_map lrm
}
