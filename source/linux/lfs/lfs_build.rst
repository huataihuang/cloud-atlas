.. _lfs_build:

=====================
LFS构建说明
=====================

我在几次LFS构建的实践中总结了一些经验，在这里做一个说明

- 将所有编译软件的版本号汇总到一个配置文件 ``lfs_vers`` ，当执行软件编译的 ``configure`` 前 **source** 这个配置信息获取当前编译软件的版本，这样命令步骤在LFS版本每次迭代时(基本)不需要修改，仅需要修订 ``lfs_vers`` 
