.. _lfs_curl:

===============
LFS CURL
===============

.. note::

   这部分不是LFS的必须，是属于BLFS的一个章节

libunistring
=================

.. literalinclude:: lfs_curl/libunistring
   :caption: 安装 libunistring

libidn2
=============

.. literalinclude:: lfs_curl/libidn2
   :caption: 安装 libidn2

libpsl
==========

libpsl包提供了访问和解析Public Suffix List(PSL)的库，这个PSL是标准后缀(例如.com)以外的一组域名。 :ref:`curl` 安装强烈建议安装libpsl

.. literalinclude:: lfs_curl/libpsl
   :caption: 安装 libpsl

curl
=========

.. literalinclude:: lfs_curl/curl
   :caption: 安装 curl


