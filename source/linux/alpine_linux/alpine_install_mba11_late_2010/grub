
# 挂载伪文件系统进入 Chroot 生成配置
mount -t proc none /mnt/proc
mount -o bind /dev /mnt/dev
mount -o bind /sys /mnt/sys

# chroot 进入系统
chroot /mnt

# 安装grub
apk add grub-bios

# 将 GRUB 写入主引导记录 MBR
grub-install --target=i386-pc --boot-directory=/boot /dev/sda

grub-mkconfig -o /boot/grub/grub.cfg
