# 确保人在解包工作区目录下
cd /tmp/initramfs_work

# 将修改后的目录树打包回 gzip 格式的 initramfs 镜像
find . -print0 | cpio --null -ov --format=newc | gzip -9 > /tmp/initramfs-lts

# 覆盖
doas cp /tmp/initramfs-lts /boot/initramfs-lts

