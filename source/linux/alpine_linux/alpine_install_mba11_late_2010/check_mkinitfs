# 1. 创建临时检查目录并进入
mkdir -p /tmp/check_initram && cd /tmp/check_initram

# 2. 解压并提取 initramfs 内容
# （注：Alpine 默认使用 gzip 压缩；如果是单 Ext4 系统，路径为 /mnt/boot/initramfs-lts）
zcat /mnt/boot/initramfs-lts | cpio -idmv 2>/dev/null

# 3. 搜索 ext4 驱动模块是否存在
find . -name "ext4.ko*"
