apk add openssh neovim \
    git sudo \
    openrc \                       # iSH环境默认没有安装openrc，安装以提供服务管理
    rsync \                        # 同步和备份文件
    util-linux                     # 提供mount,findmnt,lscpu,partx,agetty等工具(iSH虚拟环境不用安装)
