# 将 udev 服务添加到系统 boot 运行级别
sudo rc-update add udev sysinit
sudo rc-update add udev-trigger sysinit
sudo rc-update add udev-postmount default

# 立即手动启动服务（无需重启系统）
sudo rc-service udev start
sudo rc-service udev-trigger start
