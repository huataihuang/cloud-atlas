.. _raspbian_static_ip_nmcli:

=================================
使用 ``nmcli`` 配置树莓派静态IP
=================================

斗转星移，兜兜转转， :ref:`debian` / :ref:`ubuntu_linux` 现在又采用 :ref:`networkmanager` 来管理网络了。那么树莓派发行版Raspberry Pi OS也相应可以使用 ``nmcli`` 来管理配置静态IP:

.. literalinclude:: raspbian_static_ip_nmcli/nmcli
   :caption: 使用 ``nmcli`` 配置静态IP
