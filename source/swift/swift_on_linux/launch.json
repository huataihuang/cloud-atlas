{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb", // 1
      "request": "launch",
      "name": "Debug",
      "program": "${workspaceFolder}/.build/debug/Hello", // 2
      "args": ["${whoami}"], //3
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "swift-build" // 4
    }
  ]
}
