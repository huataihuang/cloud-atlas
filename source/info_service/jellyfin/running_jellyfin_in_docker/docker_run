DIR=/home/admin/docs/jellyfin

mkdir -p $DIR/conig $DIR/cache $DIR/movies

docker run -d \
  --name jellyfin \
  --net=host \
  --gpus all \
  -v $DIR/config:/config \
  -v $DIR/cache:/cache \
  -v $DIR/movies:/media/movies \
  --restart unless-stopped \
  jellyfin/jellyfin
