# 1. 杀掉当前内存中所有驻留的 tmux 守护进程（这一步会关掉所有会话，请确保重要工作已保存）
tmux kill-server 2>/dev/null || true

# 2. 如果杀得不彻底，用系统信号killall
killall tmux 2>/dev/null || true

# 3. 稳妥起见，在外面普通终端先刷新一次 Zsh 环境，确保外层的 $PATH 里 ruby 是对的
source ~/.zshrc

# 4. 重新进入 tmux
tmux
