# 从git仓库安装rbent
git clone https://github.com/rbenv/rbenv.git ~/.rbenv

# 按照shell配置rbenv环境，分别如下:
# bash:
echo 'eval "$(~/.rbenv/bin/rbenv init - bash)"' >> ~/.bashrc
# zsh:
echo 'eval "$(~/.rbenv/bin/rbenv init - zsh)"' >> ~/.zshrc
