volumes:
      - ./calibre-config:/config
      - ./library:/library       
      - ./import:/import         
      # 🪐 物理降维打击：直接把宿主机已经挂载好的 Kobo 目录，映射进容器的 /media/kobo
      - /mnt/kobo:/media/kobo:shared
